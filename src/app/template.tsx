import RouteEnterShell from "@/components/layout/RouteEnterShell";

export default function Template({ children }: { children: React.ReactNode }) {
  return <RouteEnterShell>{children}</RouteEnterShell>;
}
